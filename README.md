nodewell inventory # lists all hosts configured
nodewell ping # pings all hosts configuired
nodewell host add --name "name" --ip "ip" # adds to yaml file
nodewell host delete --name # deletes from yaml file
